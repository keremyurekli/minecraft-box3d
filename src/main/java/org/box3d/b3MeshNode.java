// Generated by jextract

package org.box3d;

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.nio.ByteOrder;
import java.util.*;
import java.util.function.*;
import java.util.stream.*;

import static java.lang.foreign.ValueLayout.*;
import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * struct b3MeshNode {
 *     b3Vec3 lowerBound;
 *     union {
 *         struct {
 *             uint32_t axis : 2;
 *             uint32_t childOffset : 30;
 *         } asNode;
 *         struct {
 *             uint32_t type : 2;
 *             uint32_t triangleCount : 30;
 *         } asLeaf;
 *     } data;
 *     b3Vec3 upperBound;
 *     uint32_t triangleOffset;
 * }
 * }
 */
public class b3MeshNode {

    b3MeshNode() {
        // Should not be called directly
    }

    private static final GroupLayout $LAYOUT = MemoryLayout.structLayout(
        b3Vec3.layout().withName("lowerBound"),
        b3MeshNode.data.layout().withName("data"),
        b3Vec3.layout().withName("upperBound"),
        Box3D.C_INT.withName("triangleOffset")
    ).withName("b3MeshNode");

    /**
     * The layout of this struct
     */
    public static final GroupLayout layout() {
        return $LAYOUT;
    }

    private static final GroupLayout lowerBound$LAYOUT = (GroupLayout)$LAYOUT.select(groupElement("lowerBound"));

    /**
     * Layout for field:
     * {@snippet lang=c :
     * b3Vec3 lowerBound
     * }
     */
    public static final GroupLayout lowerBound$layout() {
        return lowerBound$LAYOUT;
    }

    private static final long lowerBound$OFFSET = $LAYOUT.byteOffset(groupElement("lowerBound"));

    /**
     * Offset for field:
     * {@snippet lang=c :
     * b3Vec3 lowerBound
     * }
     */
    public static final long lowerBound$offset() {
        return lowerBound$OFFSET;
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * b3Vec3 lowerBound
     * }
     */
    public static MemorySegment lowerBound(MemorySegment struct) {
        return struct.asSlice(lowerBound$OFFSET, lowerBound$LAYOUT.byteSize());
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * b3Vec3 lowerBound
     * }
     */
    public static void lowerBound(MemorySegment struct, MemorySegment fieldValue) {
        MemorySegment.copy(fieldValue, 0L, struct, lowerBound$OFFSET, lowerBound$LAYOUT.byteSize());
    }

    /**
     * {@snippet lang=c :
     * union {
     *     struct {
     *         uint32_t axis : 2;
     *         uint32_t childOffset : 30;
     *     } asNode;
     *     struct {
     *         uint32_t type : 2;
     *         uint32_t triangleCount : 30;
     *     } asLeaf;
     * }
     * }
     */
    public static class data {

        data() {
            // Should not be called directly
        }

        private static final GroupLayout $LAYOUT = MemoryLayout.unionLayout(
            b3MeshNode.data.asNode.layout().withName("asNode"),
            b3MeshNode.data.asLeaf.layout().withName("asLeaf")
        ).withName("$anon$2172:2");

        /**
         * The layout of this union
         */
        public static final GroupLayout layout() {
            return $LAYOUT;
        }

        /**
         * {@snippet lang=c :
         * struct {
         *     uint32_t axis : 2;
         *     uint32_t childOffset : 30;
         * }
         * }
         */
        public static class asNode {

            asNode() {
                // Should not be called directly
            }

            private static final GroupLayout $LAYOUT = MemoryLayout.structLayout(
                MemoryLayout.paddingLayout(4)
            ).withName("$anon$2175:3");

            /**
             * The layout of this struct
             */
            public static final GroupLayout layout() {
                return $LAYOUT;
            }

            /**
             * Obtains a slice of {@code arrayParam} which selects the array element at {@code index}.
             * The returned segment has address {@code arrayParam.address() + index * layout().byteSize()}
             */
            public static MemorySegment asSlice(MemorySegment array, long index) {
                return array.asSlice(layout().byteSize() * index);
            }

            /**
             * The size (in bytes) of this struct
             */
            public static long sizeof() { return layout().byteSize(); }

            /**
             * Allocate a segment of size {@code layout().byteSize()} using {@code allocator}
             */
            public static MemorySegment allocate(SegmentAllocator allocator) {
                return allocator.allocate(layout());
            }

            /**
             * Allocate an array of size {@code elementCount} using {@code allocator}.
             * The returned segment has size {@code elementCount * layout().byteSize()}.
             */
            public static MemorySegment allocateArray(long elementCount, SegmentAllocator allocator) {
                return allocator.allocate(MemoryLayout.sequenceLayout(elementCount, layout()));
            }

            /**
             * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
             * The returned segment has size {@code layout().byteSize()}
             */
            public static MemorySegment reinterpret(MemorySegment addr, Arena arena, Consumer<MemorySegment> cleanup) {
                return reinterpret(addr, 1, arena, cleanup);
            }

            /**
             * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
             * The returned segment has size {@code elementCount * layout().byteSize()}
             */
            public static MemorySegment reinterpret(MemorySegment addr, long elementCount, Arena arena, Consumer<MemorySegment> cleanup) {
                return addr.reinterpret(layout().byteSize() * elementCount, arena, cleanup);
            }
        }

        private static final GroupLayout asNode$LAYOUT = (GroupLayout)$LAYOUT.select(groupElement("asNode"));

        /**
         * Layout for field:
         * {@snippet lang=c :
         * struct {
         *     uint32_t axis : 2;
         *     uint32_t childOffset : 30;
         * } asNode
         * }
         */
        public static final GroupLayout asNode$layout() {
            return asNode$LAYOUT;
        }

        private static final long asNode$OFFSET = $LAYOUT.byteOffset(groupElement("asNode"));

        /**
         * Offset for field:
         * {@snippet lang=c :
         * struct {
         *     uint32_t axis : 2;
         *     uint32_t childOffset : 30;
         * } asNode
         * }
         */
        public static final long asNode$offset() {
            return asNode$OFFSET;
        }

        /**
         * Getter for field:
         * {@snippet lang=c :
         * struct {
         *     uint32_t axis : 2;
         *     uint32_t childOffset : 30;
         * } asNode
         * }
         */
        public static MemorySegment asNode(MemorySegment union) {
            return union.asSlice(asNode$OFFSET, asNode$LAYOUT.byteSize());
        }

        /**
         * Setter for field:
         * {@snippet lang=c :
         * struct {
         *     uint32_t axis : 2;
         *     uint32_t childOffset : 30;
         * } asNode
         * }
         */
        public static void asNode(MemorySegment union, MemorySegment fieldValue) {
            MemorySegment.copy(fieldValue, 0L, union, asNode$OFFSET, asNode$LAYOUT.byteSize());
        }

        /**
         * {@snippet lang=c :
         * struct {
         *     uint32_t type : 2;
         *     uint32_t triangleCount : 30;
         * }
         * }
         */
        public static class asLeaf {

            asLeaf() {
                // Should not be called directly
            }

            private static final GroupLayout $LAYOUT = MemoryLayout.structLayout(
                MemoryLayout.paddingLayout(4)
            ).withName("$anon$2184:3");

            /**
             * The layout of this struct
             */
            public static final GroupLayout layout() {
                return $LAYOUT;
            }

            /**
             * Obtains a slice of {@code arrayParam} which selects the array element at {@code index}.
             * The returned segment has address {@code arrayParam.address() + index * layout().byteSize()}
             */
            public static MemorySegment asSlice(MemorySegment array, long index) {
                return array.asSlice(layout().byteSize() * index);
            }

            /**
             * The size (in bytes) of this struct
             */
            public static long sizeof() { return layout().byteSize(); }

            /**
             * Allocate a segment of size {@code layout().byteSize()} using {@code allocator}
             */
            public static MemorySegment allocate(SegmentAllocator allocator) {
                return allocator.allocate(layout());
            }

            /**
             * Allocate an array of size {@code elementCount} using {@code allocator}.
             * The returned segment has size {@code elementCount * layout().byteSize()}.
             */
            public static MemorySegment allocateArray(long elementCount, SegmentAllocator allocator) {
                return allocator.allocate(MemoryLayout.sequenceLayout(elementCount, layout()));
            }

            /**
             * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
             * The returned segment has size {@code layout().byteSize()}
             */
            public static MemorySegment reinterpret(MemorySegment addr, Arena arena, Consumer<MemorySegment> cleanup) {
                return reinterpret(addr, 1, arena, cleanup);
            }

            /**
             * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
             * The returned segment has size {@code elementCount * layout().byteSize()}
             */
            public static MemorySegment reinterpret(MemorySegment addr, long elementCount, Arena arena, Consumer<MemorySegment> cleanup) {
                return addr.reinterpret(layout().byteSize() * elementCount, arena, cleanup);
            }
        }

        private static final GroupLayout asLeaf$LAYOUT = (GroupLayout)$LAYOUT.select(groupElement("asLeaf"));

        /**
         * Layout for field:
         * {@snippet lang=c :
         * struct {
         *     uint32_t type : 2;
         *     uint32_t triangleCount : 30;
         * } asLeaf
         * }
         */
        public static final GroupLayout asLeaf$layout() {
            return asLeaf$LAYOUT;
        }

        private static final long asLeaf$OFFSET = $LAYOUT.byteOffset(groupElement("asLeaf"));

        /**
         * Offset for field:
         * {@snippet lang=c :
         * struct {
         *     uint32_t type : 2;
         *     uint32_t triangleCount : 30;
         * } asLeaf
         * }
         */
        public static final long asLeaf$offset() {
            return asLeaf$OFFSET;
        }

        /**
         * Getter for field:
         * {@snippet lang=c :
         * struct {
         *     uint32_t type : 2;
         *     uint32_t triangleCount : 30;
         * } asLeaf
         * }
         */
        public static MemorySegment asLeaf(MemorySegment union) {
            return union.asSlice(asLeaf$OFFSET, asLeaf$LAYOUT.byteSize());
        }

        /**
         * Setter for field:
         * {@snippet lang=c :
         * struct {
         *     uint32_t type : 2;
         *     uint32_t triangleCount : 30;
         * } asLeaf
         * }
         */
        public static void asLeaf(MemorySegment union, MemorySegment fieldValue) {
            MemorySegment.copy(fieldValue, 0L, union, asLeaf$OFFSET, asLeaf$LAYOUT.byteSize());
        }

        /**
         * Obtains a slice of {@code arrayParam} which selects the array element at {@code index}.
         * The returned segment has address {@code arrayParam.address() + index * layout().byteSize()}
         */
        public static MemorySegment asSlice(MemorySegment array, long index) {
            return array.asSlice(layout().byteSize() * index);
        }

        /**
         * The size (in bytes) of this union
         */
        public static long sizeof() { return layout().byteSize(); }

        /**
         * Allocate a segment of size {@code layout().byteSize()} using {@code allocator}
         */
        public static MemorySegment allocate(SegmentAllocator allocator) {
            return allocator.allocate(layout());
        }

        /**
         * Allocate an array of size {@code elementCount} using {@code allocator}.
         * The returned segment has size {@code elementCount * layout().byteSize()}.
         */
        public static MemorySegment allocateArray(long elementCount, SegmentAllocator allocator) {
            return allocator.allocate(MemoryLayout.sequenceLayout(elementCount, layout()));
        }

        /**
         * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
         * The returned segment has size {@code layout().byteSize()}
         */
        public static MemorySegment reinterpret(MemorySegment addr, Arena arena, Consumer<MemorySegment> cleanup) {
            return reinterpret(addr, 1, arena, cleanup);
        }

        /**
         * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
         * The returned segment has size {@code elementCount * layout().byteSize()}
         */
        public static MemorySegment reinterpret(MemorySegment addr, long elementCount, Arena arena, Consumer<MemorySegment> cleanup) {
            return addr.reinterpret(layout().byteSize() * elementCount, arena, cleanup);
        }
    }

    private static final GroupLayout data$LAYOUT = (GroupLayout)$LAYOUT.select(groupElement("data"));

    /**
     * Layout for field:
     * {@snippet lang=c :
     * union {
     *     struct {
     *         uint32_t axis : 2;
     *         uint32_t childOffset : 30;
     *     } asNode;
     *     struct {
     *         uint32_t type : 2;
     *         uint32_t triangleCount : 30;
     *     } asLeaf;
     * } data
     * }
     */
    public static final GroupLayout data$layout() {
        return data$LAYOUT;
    }

    private static final long data$OFFSET = $LAYOUT.byteOffset(groupElement("data"));

    /**
     * Offset for field:
     * {@snippet lang=c :
     * union {
     *     struct {
     *         uint32_t axis : 2;
     *         uint32_t childOffset : 30;
     *     } asNode;
     *     struct {
     *         uint32_t type : 2;
     *         uint32_t triangleCount : 30;
     *     } asLeaf;
     * } data
     * }
     */
    public static final long data$offset() {
        return data$OFFSET;
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * union {
     *     struct {
     *         uint32_t axis : 2;
     *         uint32_t childOffset : 30;
     *     } asNode;
     *     struct {
     *         uint32_t type : 2;
     *         uint32_t triangleCount : 30;
     *     } asLeaf;
     * } data
     * }
     */
    public static MemorySegment data(MemorySegment struct) {
        return struct.asSlice(data$OFFSET, data$LAYOUT.byteSize());
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * union {
     *     struct {
     *         uint32_t axis : 2;
     *         uint32_t childOffset : 30;
     *     } asNode;
     *     struct {
     *         uint32_t type : 2;
     *         uint32_t triangleCount : 30;
     *     } asLeaf;
     * } data
     * }
     */
    public static void data(MemorySegment struct, MemorySegment fieldValue) {
        MemorySegment.copy(fieldValue, 0L, struct, data$OFFSET, data$LAYOUT.byteSize());
    }

    private static final GroupLayout upperBound$LAYOUT = (GroupLayout)$LAYOUT.select(groupElement("upperBound"));

    /**
     * Layout for field:
     * {@snippet lang=c :
     * b3Vec3 upperBound
     * }
     */
    public static final GroupLayout upperBound$layout() {
        return upperBound$LAYOUT;
    }

    private static final long upperBound$OFFSET = $LAYOUT.byteOffset(groupElement("upperBound"));

    /**
     * Offset for field:
     * {@snippet lang=c :
     * b3Vec3 upperBound
     * }
     */
    public static final long upperBound$offset() {
        return upperBound$OFFSET;
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * b3Vec3 upperBound
     * }
     */
    public static MemorySegment upperBound(MemorySegment struct) {
        return struct.asSlice(upperBound$OFFSET, upperBound$LAYOUT.byteSize());
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * b3Vec3 upperBound
     * }
     */
    public static void upperBound(MemorySegment struct, MemorySegment fieldValue) {
        MemorySegment.copy(fieldValue, 0L, struct, upperBound$OFFSET, upperBound$LAYOUT.byteSize());
    }

    private static final OfInt triangleOffset$LAYOUT = (OfInt)$LAYOUT.select(groupElement("triangleOffset"));

    /**
     * Layout for field:
     * {@snippet lang=c :
     * uint32_t triangleOffset
     * }
     */
    public static final OfInt triangleOffset$layout() {
        return triangleOffset$LAYOUT;
    }

    private static final long triangleOffset$OFFSET = $LAYOUT.byteOffset(groupElement("triangleOffset"));

    /**
     * Offset for field:
     * {@snippet lang=c :
     * uint32_t triangleOffset
     * }
     */
    public static final long triangleOffset$offset() {
        return triangleOffset$OFFSET;
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * uint32_t triangleOffset
     * }
     */
    public static int triangleOffset(MemorySegment struct) {
        return struct.get(triangleOffset$LAYOUT, triangleOffset$OFFSET);
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * uint32_t triangleOffset
     * }
     */
    public static void triangleOffset(MemorySegment struct, int fieldValue) {
        struct.set(triangleOffset$LAYOUT, triangleOffset$OFFSET, fieldValue);
    }

    /**
     * Obtains a slice of {@code arrayParam} which selects the array element at {@code index}.
     * The returned segment has address {@code arrayParam.address() + index * layout().byteSize()}
     */
    public static MemorySegment asSlice(MemorySegment array, long index) {
        return array.asSlice(layout().byteSize() * index);
    }

    /**
     * The size (in bytes) of this struct
     */
    public static long sizeof() { return layout().byteSize(); }

    /**
     * Allocate a segment of size {@code layout().byteSize()} using {@code allocator}
     */
    public static MemorySegment allocate(SegmentAllocator allocator) {
        return allocator.allocate(layout());
    }

    /**
     * Allocate an array of size {@code elementCount} using {@code allocator}.
     * The returned segment has size {@code elementCount * layout().byteSize()}.
     */
    public static MemorySegment allocateArray(long elementCount, SegmentAllocator allocator) {
        return allocator.allocate(MemoryLayout.sequenceLayout(elementCount, layout()));
    }

    /**
     * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
     * The returned segment has size {@code layout().byteSize()}
     */
    public static MemorySegment reinterpret(MemorySegment addr, Arena arena, Consumer<MemorySegment> cleanup) {
        return reinterpret(addr, 1, arena, cleanup);
    }

    /**
     * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
     * The returned segment has size {@code elementCount * layout().byteSize()}
     */
    public static MemorySegment reinterpret(MemorySegment addr, long elementCount, Arena arena, Consumer<MemorySegment> cleanup) {
        return addr.reinterpret(layout().byteSize() * elementCount, arena, cleanup);
    }
}

